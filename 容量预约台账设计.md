# 园区容量预约台账设计

> 解决招商团队在同一园区同时向多家企业承诺用地、供电、污水处理能力时，
> 无法判断容量是否已被前序承诺占用的问题。本文档记录数据模型、业务规则与接口。

---

## 一、数据模型

### 1.1 表结构

| 表 | 作用 | 关键字段 |
|----|------|---------|
| `park_resource_capacities` | 园区资源容量主数据（只追加新版本） | `park_id` × `resource_type` × 生效月起止、`total_amount`、`unit`、`revision` |
| `capacity_reservations` | 容量预约主单 | `reservation_code`、`park_id`、`project_id`、`resource_type`、`stage`（项目阶段）、`status`、有效期间（年月） |
| `capacity_reservation_periods` | 预约的月度占用行 | `year`/`month`、`requested_amount`（确认时快照）、`confirmed_amount`、`released_amount` |
| `capacity_ledger_events` | 台账事件（追加式审计日志） | `event_type`、`operator`、`reason`、`detail`(JSON)、`occurred_at` |
| `ledger_mutex` | 台账写锁（单行） | 所有写事务的首条语句写本行，串行化并发确认 |

资源类型：用地（亩）/ 供电（千瓦）/ 污水处理（吨/日）。
预约阶段复用项目状态枚举：招商中 → 洽谈中 → 已立项 → 建设中 → 已投产。

### 1.2 预约状态机

```
暂存(DRAFT) ──确认──▶ 已确认(CONFIRMED) ──释放──▶ 已释放(RELEASED)
    │                      │
    └──作废(释放)──▶ 已释放  └──转移──▶ 已转移(TRANSFERRED)
                                       （同时生成一张新的已确认预约给目标项目）
```

- 确认动作在一笔事务内完成总量与既有承诺校验；任一月份超额则**整批拒绝**，
  响应中给出每个拒绝项的 `total_capacity`、`committed_amount`、`remaining_amount`
  以及 `sources`（来源承诺：预约号、项目、阶段、当月占用量）。
- 已确认的预约**不可原地修订**；调整只能通过释放 / 转移产生新的台账事件。
- 重复确认、重复释放、重复转移均返回 409。

## 二、核心规则

### 2.1 历史月份封存

- 业务基准月（`as_of`，缺省为当天所在月）之前的月份为**已封存**历史月份。
- 容量版本只能从当前或未来月份生效；历史版本永不修改（只追加 `revision`）。
- 预约登记 / 修订 / 确认的期间不得包含封存月份。
- 释放 / 转移只作用于未封存月份；封存月份在响应的 `skipped_closed_months`
  中列明，其台账行保持原值——历史月份的预约不会被后来的修订覆盖。

### 2.2 阶段变更与撤回的自动释放

与项目状态变更在**同一事务**内执行（挂钩：手动状态流转、提交意向、立项、里程碑推进）：

| 触发 | 规则 |
|------|------|
| 阶段前进（如 洽谈中→已立项） | 释放该项目**更早阶段**的已确认预约 |
| 阶段回退（如 已立项→洽谈中） | 释放该项目**更晚阶段**的已确认预约 |
| 项目撤回（`POST /capacity-ledger/projects/{id}/withdraw`） | 释放该项目**全部**已确认预约 |

释放只针对未使用额度（`confirmed − released`），封存月份不动；
每次自动释放写入 `规则自动释放` 事件（`detail.rule` 为
`stage_forward` / `stage_backward` / `project_withdraw`）。

### 2.3 并发控制

- 所有容量写路径（确认/释放/转移/撤回/阶段联动）先执行
  `begin_ledger_write`：若会话仅有读事务则先回滚，再以
  `INSERT OR IGNORE INTO ledger_mutex` 作为事务首条写语句。
- SQLite 因此把并发写事务串行化（连接 `timeout=30s` 忙等待），
  保证“校验总量与既有承诺”与“落库”之间不会插入其他承诺，
  也避免了共享锁→保留锁升级死锁。

## 三、接口一览（前缀 `/api/v1/capacity-ledger`）

| 方法与路径 | 说明 |
|-----------|------|
| `POST /capacities` | 登记资源容量（追加新版本） |
| `GET /capacities` | 容量版本列表 |
| `GET /parks/{park_id}/availability` | 逐月可用量：总量/已承诺/剩余/来源承诺 |
| `POST /reservations` | 登记预约（暂存） |
| `GET /reservations` / `GET /reservations/{id}` | 预约列表 / 详情（含月度行与事件） |
| `PUT /reservations/{id}` | 修订暂存预约 |
| `POST /reservations/confirm` | 批量确认（事务内校验，拒绝项含来源与剩余量） |
| `POST /reservations/{id}/release` | 释放（支持按月部分释放） |
| `POST /reservations/{id}/transfer` | 转移给同园区其他项目 |
| `GET /reservations/{id}/events` | 台账事件（审计日志） |
| `POST /projects/{project_id}/withdraw` | 项目撤回，释放全部已确认预约 |

确认响应：`status=confirmed` 或 `status=rejected`（200，整批未生效，
`rejections[]` 含来源承诺与剩余量）；对象不存在 404，违反状态/封存规则 409。

## 四、测试覆盖（`tests/test_capacity_ledger.py`）

并发确认（双线程争抢同一容量，恰有一方成功）、跨年度期间与跨年容量版本、
部分释放（按月/超额/全额）、重复操作（确认/释放/转移均 409）、
批量确认原子性、历史月份封存（登记/确认/释放/容量版本）、
阶段前进/回退自动释放、里程碑与立项联动释放、项目撤回、项目间转移、
暂存修订与确认后不可改、逐月可用量查询。
